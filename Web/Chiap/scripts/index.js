// import { LightTheme, DarkTheme, applyTheme } from './theme.js';

// // Áp dụng theme mặc định
// applyTheme(LightTheme);

// // Xử lý chuyển đổi theme khi bấm nút
// document.getElementById("toggle-theme").addEventListener("click", () => {
//     const currentTheme = document.documentElement.style.getPropertyValue('--background-color');
//     if (currentTheme === LightTheme.BACKGROUND_COLOR) {
//         applyTheme(DarkTheme);
//     } else {
//         applyTheme(LightTheme);
//     }
// });
